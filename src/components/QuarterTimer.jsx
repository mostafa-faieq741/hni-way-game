/**
 * QuarterTimer.jsx — DEPRECATED / UNUSED.
 *
 * The per-quarter timer was replaced by the global SessionTimer
 * (see ./SessionTimer.jsx), which caps total play at 30 minutes.
 * This file is no longer imported anywhere and is safe to delete.
 */
export default null
